name: Deploy Docs

on:
  push:
    branches: [main, master]

concurrency:
  group: deploy-docs-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    name: Build Docusaurus + Storybook
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Docusaurus
        working-directory: docs
        run: pnpm build

      - name: Build Storybook
        run: pnpm build-storybook --config-dir .storybook --output-dir storybook-static

      - name: Verify Storybook output is non-empty
        run: test -n "$(ls -A storybook-static)"

      - name: Merge Storybook into Docusaurus output
        run: |
          mkdir -p docs/build/storybook
          cp -r storybook-static/. docs/build/storybook/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/build

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
